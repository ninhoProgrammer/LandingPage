<div class="items-center justify-center relative z-1 max-w-3xl text-center px-8 py-12 bg-white/10 backdrop-blur-md shadow-lg" style="border-radius: 30px; background: linear-gradient(to bottom, rgba(255, 1, 1, 0.1), rgba(255, 1, 1, 0));">
    <h2 class="gradient-overlay text-4xl md:text-6xl 2xl:text-8xl font-extrabold tracking-tight drop-shadow-lg mb-6">
        Software Development
    </h2>
    <p class="text-lg md:text-xl xl:text-2xl 2xl:text-3xl font-light leading-relaxed max-w-2xl mx-auto mb-4">
        We develop your website so you can be everywhere.
    </p>
    <p class="text-sm md:text-base xl:text-lg 2xl:text-xl text-gray-600 font-light leading-relaxed max-w-2xl mx-auto mb-8">
        Trust and stability with quality 
        <span class="font-semibold text-[var(--color-primary)]">
            Web
        </span>
    </p>
    <a class="inline-flex items-center gap-3 px-8 py-4 bg-gradient-to-r from-red-300 to-red-600 hover:from-red-600 hover:to-red-300 text-white font-bold rounded-full shadow-xl transform hover:scale-105 transition duration-300" href="/diviertete">
        <span class="text-lg">
            Contact
        </span>
        <div aria-hidden="true" class="flex items-center justify-center h-[32px] w-[32px] bg-white text-red-600 rounded-full ml-1 dark:bg-[#121212] dark:text-white">
            →
        </div>
    </a>
</div>

<style>
    .gradient-overlay {
      background: linear-gradient(
        45deg,
        #ff0800,
        #3b82f6,  
        #422f4b,
        #e01515
      );
      -webkit-background-clip: text;
      background-size: 300% 300%;
      background-clip: text;
      color : transparent;
      text-transform: uppercase;
      animation: gradient 5s ease infinite;
    }
  
    @keyframes gradient {
      0% {
        background-position: 0% 50%;
      }
      50% {
        background-position: 100% 50%;
      }
      100% {
        background-position: 0% 50%;
      }
    }
</style>
